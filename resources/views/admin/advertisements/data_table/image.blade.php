<div class="d-flex align-items-center">
    <div class="symbol symbol-50px me-3">
        <img src="{{ asset($advertisement->image) }}" alt="{{ $advertisement->name }}" class="rounded">
    </div>
</div>

